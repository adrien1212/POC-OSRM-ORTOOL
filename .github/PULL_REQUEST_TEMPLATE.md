**What & why**

Closes #

**How it was verified**
- [ ] `cd backend && ./mvnw -B verify`
- [ ] `cd frontend && bun run lint && bun run build`
- [ ] Ran the stack and exercised the change in the UI

For solver changes, note the before/after on a concrete set of points (distance and duration per
vehicle).

**Notes for reviewers**
